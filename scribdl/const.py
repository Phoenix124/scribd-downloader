# Filled in by `authorize.set_credentials` after logging into a Scribd
# premium account (see the `--credentials-file` option). This allows
# access to full audiobooks and premium book content.

premium_cookies = {}
